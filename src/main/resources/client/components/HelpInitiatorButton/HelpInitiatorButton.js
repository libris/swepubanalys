'use strict'

// CSS
require('./HelpInitiatorButton.css');

/**
 * A button which can be used to trigger a help-dialog or popover
 */
var HelpInitiatorButton = {
	template: require('./HelpInitiatorButton.html')
};

module.exports = HelpInitiatorButton;